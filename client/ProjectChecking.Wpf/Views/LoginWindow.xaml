<Window x:Class="ProjectChecking.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Учёт проектов — вход"
        Height="480" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ИНТИ. Учёт проектов" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"
                   Foreground="{StaticResource PrimaryBrush}"/>

        <StackPanel Grid.Row="1" Margin="0,8">
            <TextBlock Text="Логин"/>
            <TextBox x:Name="LoginBox"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,8">
            <TextBlock Text="Пароль"/>
            <PasswordBox x:Name="PasswordBox"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,8">
            <TextBlock Text="Адрес сервера"/>
            <TextBox x:Name="ApiBox" Text="http://localhost:8080"/>
        </StackPanel>

        <TextBlock x:Name="ErrorText" Grid.Row="4" Foreground="{StaticResource DangerBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Отмена" Style="{StaticResource SecondaryButton}" Click="OnCancel"/>
            <Button x:Name="LoginButton" Content="Войти" Click="OnLogin" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
